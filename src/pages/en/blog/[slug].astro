---
import View from "../../../views/Article.astro";
import { articlesFor } from "../../../lib/data";

export async function getStaticPaths() {
  return (await articlesFor("en")).map((article) => ({ params: { slug: article.slug } }));
}
---

<View lang="en" slug={Astro.params.slug!} />
