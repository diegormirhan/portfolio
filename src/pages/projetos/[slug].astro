---
import View from "../../views/Project.astro";
import { projects } from "../../lib/content";

export function getStaticPaths() {
  return projects("pt").map((project) => ({ params: { slug: project.slug } }));
}
---

<View lang="pt" slug={Astro.params.slug!} />
